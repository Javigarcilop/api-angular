import { ProductoDisplayDirective } from './producto-display.directive';

describe('ProductoDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductoDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
